import Button from '../../common/Button';

export default function FormActions() {
  return (
    <div className="pt-4 border-t border-gray-100 flex justify-end">
      <Button type="submit">Continue to Daily Options</Button>
    </div>
  );
}
